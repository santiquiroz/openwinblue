<UserControl x:Class="OpenWinBlue.Views.StatusView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel Margin="16">
        <TextBlock Text="Connection" FontWeight="Bold" FontSize="13" Margin="0,0,0,8"/>
        <Grid Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="28"/>
                <RowDefinition Height="28"/>
                <RowDefinition Height="28"/>
                <RowDefinition Height="28"/>
                <RowDefinition Height="28"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Status:" VerticalAlignment="Center" Foreground="#888"/>
            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding ConnectionState}" VerticalAlignment="Center" FontWeight="SemiBold"/>

            <TextBlock Grid.Row="1" Grid.Column="0" Text="Active Codec:" VerticalAlignment="Center" Foreground="#888"/>
            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding ActiveCodec}" VerticalAlignment="Center" FontFamily="Consolas"/>

            <TextBlock Grid.Row="2" Grid.Column="0" Text="Bitrate:" VerticalAlignment="Center" Foreground="#888"/>
            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding BitrateText}" VerticalAlignment="Center" FontFamily="Consolas"/>

            <TextBlock Grid.Row="3" Grid.Column="0" Text="Streaming:" VerticalAlignment="Center" Foreground="#888"/>
            <Ellipse Grid.Row="3" Grid.Column="1" Width="12" Height="12" HorizontalAlignment="Left" VerticalAlignment="Center">
                <Ellipse.Style>
                    <Style TargetType="Ellipse">
                        <Setter Property="Fill" Value="#888"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsStreaming}" Value="True">
                                <Setter Property="Fill" Value="#22C55E"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Ellipse.Style>
            </Ellipse>

            <TextBlock Grid.Row="4" Grid.Column="0" Text="HFP Guard:" VerticalAlignment="Center" Foreground="#888"/>
            <TextBlock Grid.Row="4" Grid.Column="1" VerticalAlignment="Center">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Text" Value="Off"/>
                        <Setter Property="Foreground" Value="#888"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding HfpGuardActive}" Value="True">
                                <Setter Property="Text" Value="Active"/>
                                <Setter Property="Foreground" Value="#22C55E"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Grid>
        <TextBlock Text="Start owb-service.exe for live data." Foreground="#666" FontStyle="Italic" FontSize="11"/>
    </StackPanel>
</UserControl>
